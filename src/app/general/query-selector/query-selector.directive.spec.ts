import { QuerySelectorDirective } from './query-selector.directive';

describe('QuerySelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new QuerySelectorDirective();
    expect(directive).toBeTruthy();
  });
});
